using UnityEngine;

public class notas : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // usa dos muros, uno que cubra la altura normal y el segundo hacia arriba como si fuera otro tile, solo para que se sobre ponga al player
        // es una nota para mi
        

    }
}

